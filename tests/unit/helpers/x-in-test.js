import { xIn } from 'liugues/helpers/x-in';
import { module, test } from 'qunit';

module('Unit | Helper | x in');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = xIn([42]);
  assert.ok(result);
});
