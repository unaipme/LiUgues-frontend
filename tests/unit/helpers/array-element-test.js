import { arrayElement } from 'liugues/helpers/array-element';
import { module, test } from 'qunit';

module('Unit | Helper | array element');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayElement([42]);
  assert.ok(result);
});
