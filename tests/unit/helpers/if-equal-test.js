import { ifEqual } from 'liugues/helpers/if-equal';
import { module, test } from 'qunit';

module('Unit | Helper | if equal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifEqual([42]);
  assert.ok(result);
});
