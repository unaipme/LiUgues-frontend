import { filterBy } from 'liugues/helpers/filter-by';
import { module, test } from 'qunit';

module('Unit | Helper | filter by');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterBy([42]);
  assert.ok(result);
});
